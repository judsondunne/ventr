import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        position: 'absolute',
        top: 0,
        left: 0,
        right: 0,
        height: '25%', // Adjust as per your requirement
    },
    gradient: {
        flex: 1,
        resizeMode: 'cover',
    },
});

export default styles;
